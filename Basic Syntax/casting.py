x = int(1)   # x 는 1
y = int(2.8) # y 는 2
z = int("3") # z 는 3

x = float(1)     # x 는 1.0 (정수에서 실수로)
y = float(2.8)   # y 는 2.8 (실수에서 실수로)
z = float("3")   # z 는 3.0 (문자열에서 실수로)
w = float("4.2") # w 는 4.2 (문자열에서 실수로)

x = str("s1") # x will be 's1'
y = str(2)    # y will be '2'
z = str(3.0)  # z will be '3.0' 